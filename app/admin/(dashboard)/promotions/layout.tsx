import type { ReactNode } from "react";

export const metadata = { title: "Promotions · Admin" };

export default function AdminPromotionsLayout({ children }: { children: ReactNode }) {
  return children;
}
